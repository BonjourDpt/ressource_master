/**
 * Shown in the Help dialog title. Keep in sync with `package.json` `"version"`
 * (prefer `v` + semver). Optional later: generate at build time from env.
 */
export const APP_VERSION_LABEL = "v3.1.1";
